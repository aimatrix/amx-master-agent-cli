package com.aimatrix.cli.commands.hub

import com.github.ajalt.clikt.core.CliktCommand

class ApiCommand : CliktCommand(
    name = "api",
    help = "Make direct API calls to AMX-Hub"
) {
    override fun run() {
        // TODO: Implement direct API calls
    }
}